# VJ Control and Karaoke Engine dependencies
# Install with: pip install -r requirements.txt

# Environment variables from .env file
python-dotenv>=1.0.0

# Spotify API
spotipy>=2.23.0

# OSC communication (C bindings to liblo)
pyliblo3>=0.16.4

# Lyrics fetching
requests>=2.31.0

# Terminal UI
textual>=0.50.0    # Modern reactive terminal UI (vj_console.py)

# Process management
psutil>=5.9.0

# LLM support (optional - uses local Ollama by default if no OpenAI key)
openai>=1.0.0

# Vector database for shader semantic search
chromadb>=0.4.0

# MIDI I/O for MIDI router and Launchpad control
python-rtmidi>=1.5.0
mido>=1.3.0

# Launchpad Mini MK3 driver library
lpminimk3>=0.6.4

# Configuration persistence
PyYAML>=6.0

# Audio analysis and real-time DSP
sounddevice>=0.4.6  # Low-latency audio I/O via PortAudio
numpy>=1.24.0       # FFT and spectral analysis
essentia            # Beat detection, tempo, and pitch estimation

# Message schema validation (for vj_bus multi-process architecture)
pydantic>=2.0.0
