# UI Remediation Todos

- [ ] Master: remove all timing adjustment stuff from the Master UI and everywhere in code. No timing adjustment support should remain anywhere.
- [ ] Rendering: shader selection table should register clicks on the whole cell, not only on the shader name.
- [ ] Rendering: Masks list is empty even though some shaders are marked as masks.
- [ ] Rendering: image is upside down.
